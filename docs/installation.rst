============
Installation
============

At the command line::

    $ easy_install feincms_mailchimp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv feincms_mailchimp
    $ pip install feincms_mailchimp
